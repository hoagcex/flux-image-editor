export enum DGThemeType {
	DARK = "dark",
	LIGHT = "light",
	// SYSTEM = "system",
}

export enum SideBarType {
	HOVER = "HOVER",
	EXPAND = "EXPAND",
	MINIMIZE = "MINIMIZE",
	HEADER_NAVBAR = "HEADER_NAVBAR",
}
